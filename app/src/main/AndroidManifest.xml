<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.itachi1706.hackathonsg">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ProductList"
            android:label="@string/title_activity_populate_list" >
        </activity>
        <activity
            android:name=".DetailedProductDesc"
            android:label="@string/title_activity_detailed_product_desc" >
        </activity>
        <activity
            android:name=".MainSettings"
            android:label="@string/title_activity_main_settings" >
        </activity>

        <service android:name=".Services.PebbleComms"
            android:exported="false"
            tools:ignore="InnerClassSeperator" />
    </application>

</manifest>
